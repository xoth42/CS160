import example
#when you import a file, it runs that file. then you can use the functions, because they have been ran. all code will be run unless unless it is in a name==main block
example.simpleFunction()
example.printNameValue()